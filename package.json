{
  "name": "tunji-web-deux",
  "version": "2.0.4",
  "private": true,
  "workspaces": [
    "packages/common",
    "packages/client",
    "packages/server"
  ],
  "devDependencies": {
    "esbuild": "^0.25.1",
    "ts-node": "^10.9.2",
    "typescript": "^5.8.2"
  },
  "scripts": {
    "client": "yarn workspace @tunji-web/client",
    "common": "yarn workspace @tunji-web/common",
    "server": "yarn workspace @tunji-web/server",
    "build": "ts-node ./scripts/build.ts",
    "serve": "node ./packages/server/dist/index.js"
  }
}
